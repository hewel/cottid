# System-adaptive premium shell

Cottid uses one dense premium desktop layout for both light and dark modes, with
colors adapting to the system theme by default and an explicit System/Light/Dark
override in settings. This supersedes the earlier light-only shell direction:
the layout remains sidebar + central download list + selected detail rail, while
the theme layer owns color differences so UI behavior stays consistent across
system appearance changes.
